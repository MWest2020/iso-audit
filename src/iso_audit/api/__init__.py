"""Lokale audit-API — API-first auditor-flow (triage + memo).

De API is het duurzame product; frontends (minimale web nu, Nextcloud later)
zijn dunne clients. Alle beslissingen lopen via de API zodat ze herleidbaar
blijven (append-only triage-trail). Zie ``openspec/changes/auditmemo-ui/``.
"""
